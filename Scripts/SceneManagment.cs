using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// The SceneManagment class is responsible for managing different scenes in the game.
// This class currently does not implement any functionality but is set up for future scene management features.
public class SceneManagment : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // This method is called when the script instance is being loaded.
        // Any initialization logic related to the scene can be placed here.
    }

    // Update is called once per frame
    void Update()
    {
        // This method is called once per frame.
        // Any logic that needs to run continuously or checks for user input should be placed here.
    }
}
